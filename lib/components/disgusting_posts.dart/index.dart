import 'package:flutter/material.dart';

class DisgustingPostsIndex extends StatelessWidget {
  const DisgustingPostsIndex({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('test'),
    );
  }
}
